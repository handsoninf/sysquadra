<?php

$this->setup(array(
    'title' => 'Resend activation email',
    'panel' => 'centered-panel',
    'messages' => array(
        'success' => $this->activationResendMessage,
    ),
    'back' => true,
    'links' => array(
        'Login' => $this->url('user/login'),
    ),
));

?>

<div>
    <h1><?= $this->t('Reenviar Email de Ativação') ?></h1>

    <div class="separator-small"></div>

    <p>
        <?= $this->t('Se você não recebeu um e-mail de ativação após o registro, você pode solicitar um novo aqui.') ?>
        <?= $this->t('Portanto, digite o endereço de e-mail que você usou para o registro.') ?>
    </p>

    <div class="separator separator-line"></div>

    <div class="padded">

        <?php

        $form = $this->activationResendForm;
        $form->setAttribute('method', 'post');
        $form->setAttribute('action', $this->url('user/activation-resend'));
        $form->prepare();

        ?>

        <?= $this->form()->openTag($form) ?>

        <table class="default-table">
            <tr>
                <td class="default-form-label-row">
                    <?= $this->formLabel($form->get('arf-email')) ?>
                </td>
                <td>
                    <?= $this->formElement($form->get('arf-email')) ?>
                </td>
            </tr>

            <?= $this->formRowSubmit($form, 'arf-submit') ?>
        </table>

        <?= $this->form()->closeTag() ?>

    </div>
</div>